// core number utilities — baseline stub.
// Implement clamp(n, lo, hi): return n constrained to [lo, hi].
function clamp(_n, _lo, _hi) {
  throw new Error("not implemented: clamp");
}
module.exports = { clamp };
