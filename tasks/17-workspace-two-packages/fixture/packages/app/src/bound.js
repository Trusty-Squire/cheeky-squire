// app bound — baseline stub.
// Implement bound(n) = clamp(n, 0, 100) using core numbers via a RELATIVE
// require: require("../../core/src/numbers.js").
function bound(_n) {
  throw new Error("not implemented: bound");
}
module.exports = { bound };
