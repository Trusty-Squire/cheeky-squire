// app greeting — baseline stub.
// Implement greet(name) returning { slug, title } using core strings via a
// RELATIVE require: require("../../core/src/strings.js").
function greet(_name) {
  throw new Error("not implemented: greet");
}
module.exports = { greet };
