// app cli — baseline stub.
// Implement run(argv) parsing `--name X --score N` and returning
// `${greet(X).title} scored ${bound(N)}` using ./greeting.js and ./bound.js.
function run(_argv) {
  throw new Error("not implemented: run");
}
module.exports = { run };
