// Vendored dependency "jsonfmt" — v1 API (CURRENTLY INSTALLED).
// v1 format(obj) takes a single argument and always produces compact JSON.
module.exports = { version: '1.0.0', format: (obj) => JSON.stringify(obj) };
