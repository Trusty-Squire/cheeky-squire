// Encode every stored event — UNIMPLEMENTED (built by the `encode-all` node).
//
// encodeAll(store) returns store.all().map(encode) using encode from
// ./encode.js. Export encodeAll. The stub throws.
module.exports = {};
