// Encoded-event verifier — UNIMPLEMENTED (built by the `verify` node).
//
// verify(encoded) splits an encoded string on '#' into type and cc, recomputes
// the canonical checksum and returns true iff it matches the trailing cc. Only
// passes if encode (the `encode` node) is EXACTLY right. The stub throws.
module.exports = {};
