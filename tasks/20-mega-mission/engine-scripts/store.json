{
  "steps": [
    { "text": "Implementing the Store class with insertion-order add/all in src/store.js." },
    {
      "tool": "write",
      "args": {
        "path": "src/store.js",
        "content": "// In-memory event store preserving insertion order.\nclass Store {\n  constructor() {\n    this._events = [];\n  }\n  add(event) {\n    this._events.push(event);\n    return event;\n  }\n  all() {\n    return this._events.slice();\n  }\n}\nmodule.exports = { Store };\n"
      }
    },
    { "tool": "bash", "args": { "command": "node -e \"const {Store}=require('./src/store.js'); const s=new Store(); s.add({type:'a'}); s.add({type:'b'}); process.exit(s.all().length===2&&s.all()[0].type==='a'?0:1)\"" } },
    { "usage": { "in": 850, "out": 130 } },
    { "done": "Implemented Store with add/all preserving insertion order in src/store.js; verified." }
  ]
}
