// Ledger record factory. To be implemented by the `record` node.
// A record is { id: number, amountCents: number }.
// make(id, amountCents) must validate both are non-negative integers.
module.exports = {};
