{
  "steps": [
    { "text": "Implementing encodeAll(store) in src/encodeAll.js." },
    {
      "tool": "write",
      "args": {
        "path": "src/encodeAll.js",
        "content": "const { encode } = require(\"./encode.js\");\n\n// Encode every record in the store, preserving order.\nfunction encodeAll(store) {\n  return store.all().map(encode);\n}\nmodule.exports = { encodeAll };\n"
      }
    },
    { "tool": "bash", "args": { "command": "node -e \"const {encodeAll}=require('./src/encodeAll.js'); const {Store}=require('./src/store.js'); const {make}=require('./src/record.js'); const s=new Store(); s.add(make(7,50)); process.exit(encodeAll(s)[0]==='7|50|57'?0:1)\"" } },
    { "usage": { "in": 1000, "out": 110 } },
    { "done": "Implemented encodeAll() mapping encode over store.all() in src/encodeAll.js; verified." }
  ]
}
