thesis: "Train a GPT-4-class foundation model from scratch for $100."
requirements:
  - id: R1
    statement: "model matches GPT-4 benchmarks, trained from scratch"
    acceptance: { tier: 1, gate: "npm run test" }
decisions:
  - id: D1
    statement: "train on a single rented GPU over a weekend"
    rationale: "efficient architectures changed the economics"
    claims: [C1]
claims:
  - id: C1
    statement: "$100 of compute suffices to train a GPT-4-class model from scratch"
    status: unverified
